A simple REST API built using json server. 

Postman was used as well for testing POST and PUT request. 

I plan to have the API hosted on heroku in the future so that viewers can view and use the API. 
